VITE_API_URL=http://localhost:4100/api
# En local no hay subdominios reales; este header le indica al backend qué tienda de prueba usar
# (ver saas-backend/src/middleware/tenantResolution.js). No tiene efecto en producción.
VITE_DEV_TENANT_SUBDOMAIN=demo
